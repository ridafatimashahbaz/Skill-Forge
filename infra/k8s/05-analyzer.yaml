apiVersion: apps/v1
kind: Deployment
metadata:
  name: analyzer
  namespace: skillforge
spec:
  replicas: 1
  selector:
    matchLabels: { app: analyzer }
  template:
    metadata:
      labels: { app: analyzer }
    spec:
      containers:
        - name: analyzer
          image: skillforge-analyzer:latest
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: skillforge-config }
          env:
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: skillforge-secrets, key: JWT_SECRET } }
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analyzer
  namespace: skillforge
spec:
  selector: { app: analyzer }
  ports: [{ port: 8000, targetPort: 8000 }]
