apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai
  namespace: skillforge
spec:
  replicas: 1
  selector:
    matchLabels: { app: ai }
  template:
    metadata:
      labels: { app: ai }
    spec:
      containers:
        - name: ai
          image: skillforge-ai:latest
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: skillforge-config }
          env:
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: skillforge-secrets, key: JWT_SECRET } }
            - name: ANTHROPIC_API_KEY
              valueFrom: { secretKeyRef: { name: skillforge-secrets, key: ANTHROPIC_API_KEY } }
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: skillforge
spec:
  selector: { app: ai }
  ports: [{ port: 8000, targetPort: 8000 }]
