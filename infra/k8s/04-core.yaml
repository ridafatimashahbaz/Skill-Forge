apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
  namespace: skillforge
spec:
  replicas: 1
  selector:
    matchLabels: { app: core }
  template:
    metadata:
      labels: { app: core }
    spec:
      containers:
        - name: core
          image: skillforge-core:latest
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: skillforge-config }
          env:
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: skillforge-secrets, key: JWT_SECRET } }
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: skillforge
spec:
  selector: { app: core }
  ports: [{ port: 8000, targetPort: 8000 }]
